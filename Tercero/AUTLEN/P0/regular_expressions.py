
"""
Esta es la expresion regular para el ejercicio 0, que se facilita
a modo de ejemplo:
"""
RE0 = "[a-zA-Z]+"

"""
Completa a continuacion las expresiones regulares para los
ejercicios 1-5:
"""
RE1 = "[a-zA-Z0-9_]+\.py"
RE2 = "((((^-([0-9]+)*)|([1-9]))+)*(\.([0-9]+)*)*)|(0(\.[0-9]+)*)"
RE3 = "[a-z]+\.[a-z]+(@uam\.es|@estudiante\.uam\.es)"
RE4 = "[a-z \(\)]*\([a-z]*\)[a-z ]*"
RE5 = "[a-z \(\)]*\([a-z]*\)[a-z ]*"

"""
Recuerda que puedes usar el fichero prueba.py para probar tus
expresiones regulares.
"""

""" 
EJERCICIO 6:
Incluye a continuacion, dentro de esta cadena, tu respuesta 
al ejercicio 6.

-Porque las expresiones regulares no son lo suficientemente potentes como 
para poder hacer coincidir secuencias de paréntesis anidados, 
ni en general construcciones anidadas arbitrariamente, 
para poder hacer esto habría que utilizar un autómata.

"""
